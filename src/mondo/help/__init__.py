"""Bundled help topics for `mondo help <topic>`.

Markdown files co-located in this package ship inside the wheel / PyInstaller
binary via `importlib.resources`, so the CLI is fully self-contained — no
internet, no repo checkout required.
"""
